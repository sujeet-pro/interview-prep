import { assertEquals } from "https://deno.land/std/testing/asserts.ts";
export function sol(): null {
  return null;
}

Deno.test("base case", () => {
  const res = sol();
  assertEquals(res, null);
});
